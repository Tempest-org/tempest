syntax = "proto3";

package access;
option go_package="./access";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Access {
  rpc Ping(Request) returns(Response);
}
